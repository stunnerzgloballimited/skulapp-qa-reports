# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: journeys/admin-panel/dashboard-panel.spec.ts >> Administrator dashboard panel journey >> administrator dashboard exposes the current panel, header, profile, weather, and key module routes
- Location: tests/journeys/admin-panel/dashboard-panel.spec.ts:72:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/Good (Morning|Afternoon|Evening)/i).first()
Expected: visible
Timeout: 30000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 30000ms
  - waiting for getByText(/Good (Morning|Afternoon|Evening)/i).first()

```

# Test source

```ts
  1   | import { expect, test, type Page, type TestInfo } from '@playwright/test';
  2   | import { qaConfig, urlFor } from '../../../src/env';
  3   | import { adminAuthStatePath } from '../../../src/journey-state';
  4   | 
  5   | const adminModules = [
  6   |   'Discuss',
  7   |   'Calendar',
  8   |   'Social Media',
  9   |   'Analytics',
  10  |   'Onboarding',
  11  |   'Teachers',
  12  |   'Students',
  13  |   'Admissions',
  14  |   'Accounting',
  15  |   'Notice Board',
  16  |   'Timetables',
  17  |   'Activity Logs',
  18  |   'Settings',
  19  |   'Parents',
  20  |   'Subjects',
  21  |   'Leave Types',
  22  |   'Fee Structures',
  23  |   'Sections',
  24  | ];
  25  | 
  26  | async function attachScreenshot(page: Page, testInfo: TestInfo, name: string) {
  27  |   await testInfo.attach(name, {
  28  |     body: await page.screenshot({ fullPage: true }),
  29  |     contentType: 'image/png',
  30  |   });
  31  | }
  32  | 
  33  | async function openDashboard(page: Page) {
  34  |   await page.goto(urlFor(qaConfig.dashboardBaseUrl, '/'));
> 35  |   await expect(page.getByText(/Good (Morning|Afternoon|Evening)/i).first()).toBeVisible({ timeout: 30_000 });
      |                                                                             ^ Error: expect(locator).toBeVisible() failed
  36  |   await expect(page.getByText('Dashboard', { exact: true }).first()).toBeVisible();
  37  | }
  38  | 
  39  | async function clickDashboardTile(page: Page, title: string) {
  40  |   await page.getByText(title, { exact: true }).last().click();
  41  | }
  42  | 
  43  | async function dismissTestimonialPrompt(page: Page) {
  44  |   const askLater = page.getByRole('button', { name: /Ask me later/i });
  45  |   const close = page.getByRole('button', { name: /^Close$/i });
  46  | 
  47  |   const promptAppeared = await page
  48  |     .locator('[data-component-file="TestimonialPromptModal.jsx"]')
  49  |     .first()
  50  |     .waitFor({ state: 'visible', timeout: 2_000 })
  51  |     .then(() => true)
  52  |     .catch(() => false);
  53  | 
  54  |   if (!promptAppeared) return;
  55  | 
  56  |   if (await askLater.isVisible().catch(() => false)) {
  57  |     await askLater.click();
  58  |     await expect(askLater).toBeHidden({ timeout: 10_000 });
  59  |     return;
  60  |   }
  61  | 
  62  |   if (await close.isVisible().catch(() => false)) {
  63  |     await close.click();
  64  |     await expect(close).toBeHidden({ timeout: 10_000 });
  65  |   }
  66  | }
  67  | 
  68  | test.describe('Administrator dashboard panel journey', () => {
  69  |   test.setTimeout(120_000);
  70  |   test.skip(!qaConfig.runSchoolRegistrationJourney, 'Enable with RUN_SCHOOL_REGISTRATION_JOURNEY=true.');
  71  | 
  72  |   test('administrator dashboard exposes the current panel, header, profile, weather, and key module routes', async ({
  73  |     browser,
  74  |   }, testInfo) => {
  75  |     const context = await browser.newContext({ storageState: adminAuthStatePath() });
  76  |     const page = await context.newPage();
  77  | 
  78  |     try {
  79  |       await openDashboard(page);
  80  |       await expect(page.getByText(/Administrator Profile Setup/i)).toBeHidden({ timeout: 10_000 });
  81  |       await dismissTestimonialPrompt(page);
  82  | 
  83  |       for (const label of ['Dashboard', 'Analytics', 'Accounting', 'Apps', 'Gallery', 'Settings', 'More']) {
  84  |         await expect(page.locator('header').getByText(label, { exact: true })).toBeVisible();
  85  |       }
  86  | 
  87  |       await expect(page.getByLabel(/Open weather forecast/i)).toBeVisible();
  88  |       await expect(page.locator('header').getByText(/Administrator/i).first()).toBeVisible();
  89  | 
  90  |       for (const moduleName of adminModules) {
  91  |         await expect(page.getByText(moduleName, { exact: true }).last()).toBeVisible();
  92  |       }
  93  | 
  94  |       await expect(page.getByText(/School Anthem/i)).toBeVisible();
  95  |       await attachScreenshot(page, testInfo, '01-admin-dashboard-current-panel');
  96  | 
  97  |       await dismissTestimonialPrompt(page);
  98  |       await page.locator('header').getByText('More', { exact: true }).click();
  99  |       await expect(page.getByText('Timetable', { exact: true })).toBeVisible();
  100 |       await expect(page.getByText('Activity Logs', { exact: true }).first()).toBeVisible();
  101 |       await attachScreenshot(page, testInfo, '02-admin-header-more-menu');
  102 | 
  103 |       const routeChecks = [
  104 |         { tile: 'Teachers', path: /\/teachers$/, heading: /^Teachers$/i },
  105 |         { tile: 'Students', path: /\/students$/, heading: /^Students$/i },
  106 |         { tile: 'Parents', path: /\/parents$/, heading: /^Parents$/i },
  107 |         { tile: 'Activity Logs', path: /\/activity-logs$/, heading: /^Activity Logs$/i },
  108 |       ];
  109 | 
  110 |       for (const route of routeChecks) {
  111 |         await openDashboard(page);
  112 |         await dismissTestimonialPrompt(page);
  113 |         await clickDashboardTile(page, route.tile);
  114 |         await expect(page).toHaveURL(route.path, { timeout: 20_000 });
  115 |         await expect(page.getByRole('heading', { name: route.heading }).first()).toBeVisible({ timeout: 20_000 });
  116 |         await attachScreenshot(page, testInfo, `route-${route.tile.toLowerCase().replace(/\s+/g, '-')}`);
  117 |       }
  118 |     } finally {
  119 |       await context.close();
  120 |     }
  121 |   });
  122 | });
  123 | 
```