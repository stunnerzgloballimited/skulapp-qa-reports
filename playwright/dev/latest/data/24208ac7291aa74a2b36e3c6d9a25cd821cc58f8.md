# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: journeys/calendar/calendar.spec.ts >> Calendar journey >> administrator can create, open, and delete a calendar event
- Location: tests/journeys/calendar/calendar.spec.ts:32:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByTestId('calendar-page')
Expected: visible
Timeout: 30000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 30000ms
  - waiting for getByTestId('calendar-page')
    2 × waiting for" https://dev.skulapp.com/login" navigation to finish...
      - navigated to "https://dashboard.dev.skulapp.com/"

```

# Test source

```ts
  1  | import { expect, test, type Page, type TestInfo } from '@playwright/test';
  2  | import { qaConfig, urlFor } from '../../../src/env';
  3  | import { adminAuthStatePath } from '../../../src/journey-state';
  4  | 
  5  | async function attachScreenshot(page: Page, testInfo: TestInfo, name: string) {
  6  |   await testInfo.attach(name, {
  7  |     body: await page.screenshot({ fullPage: true }),
  8  |     contentType: 'image/png',
  9  |   });
  10 | }
  11 | 
  12 | async function dismissTestimonialPrompt(page: Page) {
  13 |   const askLater = page.getByRole('button', { name: /Ask me later/i });
  14 |   const close = page.getByRole('button', { name: /^Close$/i });
  15 | 
  16 |   if (await askLater.isVisible().catch(() => false)) {
  17 |     await askLater.click();
  18 |     await expect(askLater).toBeHidden({ timeout: 10_000 });
  19 |     return;
  20 |   }
  21 | 
  22 |   if (await close.isVisible().catch(() => false)) {
  23 |     await close.click();
  24 |     await expect(close).toBeHidden({ timeout: 10_000 });
  25 |   }
  26 | }
  27 | 
  28 | test.describe('Calendar journey', () => {
  29 |   test.setTimeout(120_000);
  30 |   test.skip(!qaConfig.runSchoolRegistrationJourney, 'Enable with RUN_SCHOOL_REGISTRATION_JOURNEY=true.');
  31 | 
  32 |   test('administrator can create, open, and delete a calendar event', async ({ browser }, testInfo) => {
  33 |     const context = await browser.newContext({ storageState: adminAuthStatePath() });
  34 |     const page = await context.newPage();
  35 |     const eventTitle = 'QA Calendar Review';
  36 | 
  37 |     try {
  38 |       await page.goto(urlFor(qaConfig.dashboardBaseUrl, '/calendar'));
> 39 |       await expect(page.getByTestId('calendar-page')).toBeVisible({ timeout: 30_000 });
     |                                                       ^ Error: expect(locator).toBeVisible() failed
  40 |       await expect(page.getByRole('link', { name: /Calendar/i })).toBeVisible();
  41 |       await dismissTestimonialPrompt(page);
  42 |       await attachScreenshot(page, testInfo, '01-calendar-page-ready');
  43 | 
  44 |       await page.getByTestId('calendar-new-event').click();
  45 |       await expect(page.getByRole('heading', { name: /Create event/i })).toBeVisible();
  46 |       await page.locator('[name="title"]').fill(eventTitle);
  47 |       await page.locator('[name="type"]').selectOption('meeting');
  48 |       await page.locator('[name="audience"]').selectOption('Teachers');
  49 |       await page.locator('[name="date"]').fill('2026-05-05');
  50 |       await page.locator('[name="startTime"]').fill('10:00');
  51 |       await page.locator('[name="endDate"]').fill('2026-05-05');
  52 |       await page.locator('[name="endTime"]').fill('11:00');
  53 |       await page.locator('[name="location"]').fill('Staff Room');
  54 |       await page
  55 |         .locator('textarea[name="description"]')
  56 |         .fill('A test calendar event used to verify the SkulApp calendar create and details flow.');
  57 |       await attachScreenshot(page, testInfo, '02-calendar-event-form-filled');
  58 | 
  59 |       await page.getByRole('button', { name: /Save event/i }).click();
  60 |       await expect(page.getByRole('heading', { name: /Create event/i })).toBeHidden({ timeout: 15_000 });
  61 |       await expect(page.locator('.fc-event').filter({ hasText: eventTitle })).toBeVisible({ timeout: 15_000 });
  62 |       await attachScreenshot(page, testInfo, '03-calendar-event-created');
  63 | 
  64 |       await page.locator('.fc-event').filter({ hasText: eventTitle }).click();
  65 |       await expect(page.getByTestId('calendar-event-details')).toContainText(eventTitle);
  66 |       await expect(page.getByTestId('calendar-event-details')).toContainText('Staff Room');
  67 |       await attachScreenshot(page, testInfo, '04-calendar-event-details-open');
  68 | 
  69 |       await page.getByTestId('calendar-event-details').getByRole('button', { name: /^Delete$/i }).click();
  70 |       await expect(page.getByText(eventTitle)).toHaveCount(0);
  71 |       await attachScreenshot(page, testInfo, '05-calendar-event-deleted');
  72 |     } finally {
  73 |       await context.close();
  74 |     }
  75 |   });
  76 | });
  77 | 
```